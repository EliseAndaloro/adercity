{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h1>Photo</h1>
    <h2>{{ photo.title }}</h2>
    <img src="{{ asset('/uploads/'~photo.filename) }}" alt="">

    <a href="{{ path('photo_index') }}">back to list</a>

    {% if is_granted('ROLE_ADMIN') %}
        {{ include('photo/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
