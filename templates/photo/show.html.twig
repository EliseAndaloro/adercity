{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h2 class="text-center mt-5" >{{ photo.title }}</h2>
    <div>
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('photo/_delete_form.html.twig') }}
        {% endif %}
    </div>
        <a class="btn btn-success" href="{{ path('photo_index') }}">Back to list</a>
    <div class="text-center">
        <img id="show_image" src="{{ asset('/uploads/'~photo.filename) }}" alt="">
    </div>

{% endblock %}
